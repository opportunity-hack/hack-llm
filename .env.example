# Secrets inventory (PLAN.md §5) — set these as Fly secrets, NEVER commit values.
#   fly secrets set -a ohack-ai-gateway KEY=value [KEY=value ...]

# Gateway admin (generate: python3 -c "import secrets; print('sk-ohack-'+secrets.token_urlsafe(24))")
LITELLM_MASTER_KEY=
UI_USERNAME=admin
UI_PASSWORD=

# Set automatically by `fly postgres attach` — don't set by hand
# DATABASE_URL=

# Lane 1 — Meta Model API (dev.meta.ai), fund + set $400 provider-side limit
META_MODEL_API_KEY=

# Lanes 2+3 — Moonshot (platform.moonshot.ai), fund $600 + set hard spend limit
MOONSHOT_API_KEY=

# Lane 4 — free tiers (verify ToS permits pooled use before enabling; PLAN §5)
GROQ_API_KEY=
CEREBRAS_API_KEY=
NVIDIA_NIM_API_KEY=

# Escape hatch (optional, PLAN §5): openrouter.ai prefunded $50
# OPENROUTER_API_KEY=
